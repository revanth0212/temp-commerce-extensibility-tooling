{
  "name": "aio-where",
  "description": "Show current Adobe I/O configuration (org, project, workspace) and ask for deployment confirmation.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "verbose": {
        "type": "boolean",
        "description": "Enable verbose output",
        "default": false
      }
    },
    "required": []
  }
} 